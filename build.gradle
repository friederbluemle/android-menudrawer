buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.6.+'
    }
}

ext.groupId = 'net.simonvt.messagebar'
ext.compileSdkVersion = 21
ext.buildToolsVersion = "21.0.2"
ext.minSdkVersion = 7;
ext.targetSdkVersion = 21;

allprojects {
    repositories {
        mavenCentral()
    }

    tasks.withType(Compile) {
        options.encoding = "UTF-8"
    }

    version = '3.0.5-SNAPSHOT'
}
